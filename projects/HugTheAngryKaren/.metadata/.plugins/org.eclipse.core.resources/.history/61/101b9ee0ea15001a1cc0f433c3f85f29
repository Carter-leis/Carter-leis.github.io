/**
 * Boardable
 * @author carterleising
 * an interface of boardable.
 * stylus implements this
 */
public interface Boardable {

	/**
	 * shows if the boardable object is visible or not
	 * @return returns the visibility of the boarable object
	 */
	public boolean isVisible();
}
